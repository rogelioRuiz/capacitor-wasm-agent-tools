// This file was autogenerated by some hot garbage in the `uniffi` crate.
// Trust me, you don't want to mess with it!

// swiftlint:disable all
import Foundation

// Depending on the consumer's build setup, the low-level FFI code
// might be in a separate module, or it might be compiled inline into
// this module. This is a bit of light hackery to work with both.
#if canImport(wasm_agent_tools_ffiFFI)
import wasm_agent_tools_ffiFFI
#endif

fileprivate extension RustBuffer {
    // Allocate a new buffer, copying the contents of a `UInt8` array.
    init(bytes: [UInt8]) {
        let rbuf = bytes.withUnsafeBufferPointer { ptr in
            RustBuffer.from(ptr)
        }
        self.init(capacity: rbuf.capacity, len: rbuf.len, data: rbuf.data)
    }

    static func empty() -> RustBuffer {
        RustBuffer(capacity: 0, len:0, data: nil)
    }

    static func from(_ ptr: UnsafeBufferPointer<UInt8>) -> RustBuffer {
        try! rustCall { ffi_wasm_agent_tools_ffi_rustbuffer_from_bytes(ForeignBytes(bufferPointer: ptr), $0) }
    }

    // Frees the buffer in place.
    // The buffer must not be used after this is called.
    func deallocate() {
        try! rustCall { ffi_wasm_agent_tools_ffi_rustbuffer_free(self, $0) }
    }
}

fileprivate extension ForeignBytes {
    init(bufferPointer: UnsafeBufferPointer<UInt8>) {
        self.init(len: Int32(bufferPointer.count), data: bufferPointer.baseAddress)
    }
}

// For every type used in the interface, we provide helper methods for conveniently
// lifting and lowering that type from C-compatible data, and for reading and writing
// values of that type in a buffer.

// Helper classes/extensions that don't change.
// Someday, this will be in a library of its own.

fileprivate extension Data {
    init(rustBuffer: RustBuffer) {
        self.init(
            bytesNoCopy: rustBuffer.data!,
            count: Int(rustBuffer.len),
            deallocator: .none
        )
    }
}

// Define reader functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.
//
// With external types, one swift source file needs to be able to call the read
// method on another source file's FfiConverter, but then what visibility
// should Reader have?
// - If Reader is fileprivate, then this means the read() must also
//   be fileprivate, which doesn't work with external types.
// - If Reader is internal/public, we'll get compile errors since both source
//   files will try define the same type.
//
// Instead, the read() method and these helper functions input a tuple of data

fileprivate func createReader(data: Data) -> (data: Data, offset: Data.Index) {
    (data: data, offset: 0)
}

// Reads an integer at the current offset, in big-endian order, and advances
// the offset on success. Throws if reading the integer would move the
// offset past the end of the buffer.
fileprivate func readInt<T: FixedWidthInteger>(_ reader: inout (data: Data, offset: Data.Index)) throws -> T {
    let range = reader.offset..<reader.offset + MemoryLayout<T>.size
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    if T.self == UInt8.self {
        let value = reader.data[reader.offset]
        reader.offset += 1
        return value as! T
    }
    var value: T = 0
    let _ = withUnsafeMutableBytes(of: &value, { reader.data.copyBytes(to: $0, from: range)})
    reader.offset = range.upperBound
    return value.bigEndian
}

// Reads an arbitrary number of bytes, to be used to read
// raw bytes, this is useful when lifting strings
fileprivate func readBytes(_ reader: inout (data: Data, offset: Data.Index), count: Int) throws -> Array<UInt8> {
    let range = reader.offset..<(reader.offset+count)
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    var value = [UInt8](repeating: 0, count: count)
    value.withUnsafeMutableBufferPointer({ buffer in
        reader.data.copyBytes(to: buffer, from: range)
    })
    reader.offset = range.upperBound
    return value
}

// Reads a float at the current offset.
fileprivate func readFloat(_ reader: inout (data: Data, offset: Data.Index)) throws -> Float {
    return Float(bitPattern: try readInt(&reader))
}

// Reads a float at the current offset.
fileprivate func readDouble(_ reader: inout (data: Data, offset: Data.Index)) throws -> Double {
    return Double(bitPattern: try readInt(&reader))
}

// Indicates if the offset has reached the end of the buffer.
fileprivate func hasRemaining(_ reader: (data: Data, offset: Data.Index)) -> Bool {
    return reader.offset < reader.data.count
}

// Define writer functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.  See the above discussion on Readers for details.

fileprivate func createWriter() -> [UInt8] {
    return []
}

fileprivate func writeBytes<S>(_ writer: inout [UInt8], _ byteArr: S) where S: Sequence, S.Element == UInt8 {
    writer.append(contentsOf: byteArr)
}

// Writes an integer in big-endian order.
//
// Warning: make sure what you are trying to write
// is in the correct type!
fileprivate func writeInt<T: FixedWidthInteger>(_ writer: inout [UInt8], _ value: T) {
    var value = value.bigEndian
    withUnsafeBytes(of: &value) { writer.append(contentsOf: $0) }
}

fileprivate func writeFloat(_ writer: inout [UInt8], _ value: Float) {
    writeInt(&writer, value.bitPattern)
}

fileprivate func writeDouble(_ writer: inout [UInt8], _ value: Double) {
    writeInt(&writer, value.bitPattern)
}

// Protocol for types that transfer other types across the FFI. This is
// analogous to the Rust trait of the same name.
fileprivate protocol FfiConverter {
    associatedtype FfiType
    associatedtype SwiftType

    static func lift(_ value: FfiType) throws -> SwiftType
    static func lower(_ value: SwiftType) -> FfiType
    static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType
    static func write(_ value: SwiftType, into buf: inout [UInt8])
}

// Types conforming to `Primitive` pass themselves directly over the FFI.
fileprivate protocol FfiConverterPrimitive: FfiConverter where FfiType == SwiftType { }

extension FfiConverterPrimitive {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ value: FfiType) throws -> SwiftType {
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> FfiType {
        return value
    }
}

// Types conforming to `FfiConverterRustBuffer` lift and lower into a `RustBuffer`.
// Used for complex types where it's hard to write a custom lift/lower.
fileprivate protocol FfiConverterRustBuffer: FfiConverter where FfiType == RustBuffer {}

extension FfiConverterRustBuffer {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ buf: RustBuffer) throws -> SwiftType {
        var reader = createReader(data: Data(rustBuffer: buf))
        let value = try read(from: &reader)
        if hasRemaining(reader) {
            throw UniffiInternalError.incompleteData
        }
        buf.deallocate()
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> RustBuffer {
          var writer = createWriter()
          write(value, into: &writer)
          return RustBuffer(bytes: writer)
    }
}
// An error type for FFI errors. These errors occur at the UniFFI level, not
// the library level.
fileprivate enum UniffiInternalError: LocalizedError {
    case bufferOverflow
    case incompleteData
    case unexpectedOptionalTag
    case unexpectedEnumCase
    case unexpectedNullPointer
    case unexpectedRustCallStatusCode
    case unexpectedRustCallError
    case unexpectedStaleHandle
    case rustPanic(_ message: String)

    public var errorDescription: String? {
        switch self {
        case .bufferOverflow: return "Reading the requested value would read past the end of the buffer"
        case .incompleteData: return "The buffer still has data after lifting its containing value"
        case .unexpectedOptionalTag: return "Unexpected optional tag; should be 0 or 1"
        case .unexpectedEnumCase: return "Raw enum value doesn't match any cases"
        case .unexpectedNullPointer: return "Raw pointer value was null"
        case .unexpectedRustCallStatusCode: return "Unexpected RustCallStatus code"
        case .unexpectedRustCallError: return "CALL_ERROR but no errorClass specified"
        case .unexpectedStaleHandle: return "The object in the handle map has been dropped already"
        case let .rustPanic(message): return message
        }
    }
}

fileprivate extension NSLock {
    func withLock<T>(f: () throws -> T) rethrows -> T {
        self.lock()
        defer { self.unlock() }
        return try f()
    }
}

fileprivate let CALL_SUCCESS: Int8 = 0
fileprivate let CALL_ERROR: Int8 = 1
fileprivate let CALL_UNEXPECTED_ERROR: Int8 = 2
fileprivate let CALL_CANCELLED: Int8 = 3

fileprivate extension RustCallStatus {
    init() {
        self.init(
            code: CALL_SUCCESS,
            errorBuf: RustBuffer.init(
                capacity: 0,
                len: 0,
                data: nil
            )
        )
    }
}

private func rustCall<T>(_ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    let neverThrow: ((RustBuffer) throws -> Never)? = nil
    return try makeRustCall(callback, errorHandler: neverThrow)
}

private func rustCallWithError<T, E: Swift.Error>(
    _ errorHandler: @escaping (RustBuffer) throws -> E,
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    try makeRustCall(callback, errorHandler: errorHandler)
}

private func makeRustCall<T, E: Swift.Error>(
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T,
    errorHandler: ((RustBuffer) throws -> E)?
) throws -> T {
    uniffiEnsureInitialized()
    var callStatus = RustCallStatus.init()
    let returnedVal = callback(&callStatus)
    try uniffiCheckCallStatus(callStatus: callStatus, errorHandler: errorHandler)
    return returnedVal
}

private func uniffiCheckCallStatus<E: Swift.Error>(
    callStatus: RustCallStatus,
    errorHandler: ((RustBuffer) throws -> E)?
) throws {
    switch callStatus.code {
        case CALL_SUCCESS:
            return

        case CALL_ERROR:
            if let errorHandler = errorHandler {
                throw try errorHandler(callStatus.errorBuf)
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.unexpectedRustCallError
            }

        case CALL_UNEXPECTED_ERROR:
            // When the rust code sees a panic, it tries to construct a RustBuffer
            // with the message.  But if that code panics, then it just sends back
            // an empty buffer.
            if callStatus.errorBuf.len > 0 {
                throw UniffiInternalError.rustPanic(try FfiConverterString.lift(callStatus.errorBuf))
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.rustPanic("Rust panic")
            }

        case CALL_CANCELLED:
            fatalError("Cancellation not supported yet")

        default:
            throw UniffiInternalError.unexpectedRustCallStatusCode
    }
}

private func uniffiTraitInterfaceCall<T>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> ()
) {
    do {
        try writeReturn(makeCall())
    } catch let error {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}

private func uniffiTraitInterfaceCallWithError<T, E>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> (),
    lowerError: (E) -> RustBuffer
) {
    do {
        try writeReturn(makeCall())
    } catch let error as E {
        callStatus.pointee.code = CALL_ERROR
        callStatus.pointee.errorBuf = lowerError(error)
    } catch {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}
fileprivate class UniffiHandleMap<T> {
    private var map: [UInt64: T] = [:]
    private let lock = NSLock()
    private var currentHandle: UInt64 = 1

    func insert(obj: T) -> UInt64 {
        lock.withLock {
            let handle = currentHandle
            currentHandle += 1
            map[handle] = obj
            return handle
        }
    }

     func get(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map[handle] else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

    @discardableResult
    func remove(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map.removeValue(forKey: handle) else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

    var count: Int {
        get {
            map.count
        }
    }
}


// Public interface members begin here.


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt64: FfiConverterPrimitive {
    typealias FfiType = UInt64
    typealias SwiftType = UInt64

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt64 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterString: FfiConverter {
    typealias SwiftType = String
    typealias FfiType = RustBuffer

    public static func lift(_ value: RustBuffer) throws -> String {
        defer {
            value.deallocate()
        }
        if value.data == nil {
            return String()
        }
        let bytes = UnsafeBufferPointer<UInt8>(start: value.data!, count: Int(value.len))
        return String(bytes: bytes, encoding: String.Encoding.utf8)!
    }

    public static func lower(_ value: String) -> RustBuffer {
        return value.utf8CString.withUnsafeBufferPointer { ptr in
            // The swift string gives us int8_t, we want uint8_t.
            ptr.withMemoryRebound(to: UInt8.self) { ptr in
                // The swift string gives us a trailing null byte, we don't want it.
                let buf = UnsafeBufferPointer(rebasing: ptr.prefix(upTo: ptr.count - 1))
                return RustBuffer.from(buf)
            }
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> String {
        let len: Int32 = try readInt(&buf)
        return String(bytes: try readBytes(&buf, count: Int(len)), encoding: String.Encoding.utf8)!
    }

    public static func write(_ value: String, into buf: inout [UInt8]) {
        let len = Int32(value.utf8.count)
        writeInt(&buf, len)
        writeBytes(&buf, value.utf8)
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterData: FfiConverterRustBuffer {
    typealias SwiftType = Data

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Data {
        let len: Int32 = try readInt(&buf)
        return Data(try readBytes(&buf, count: Int(len)))
    }

    public static func write(_ value: Data, into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        writeBytes(&buf, value)
    }
}




public protocol JsRuntimeProtocol : AnyObject {
    
    func execute(code: String, config: JsConfig) throws  -> JsResult
    
}

open class JsRuntime:
    JsRuntimeProtocol {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_wasm_agent_tools_ffi_fn_clone_jsruntime(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_wasm_agent_tools_ffi_fn_free_jsruntime(pointer, $0) }
    }

    
public static func create()throws  -> JsRuntime {
    return try  FfiConverterTypeJsRuntime.lift(try rustCallWithError(FfiConverterTypeWasmToolsError.lift) {
    uniffi_wasm_agent_tools_ffi_fn_constructor_jsruntime_create($0
    )
})
}
    

    
open func execute(code: String, config: JsConfig)throws  -> JsResult {
    return try  FfiConverterTypeJsResult.lift(try rustCallWithError(FfiConverterTypeWasmToolsError.lift) {
    uniffi_wasm_agent_tools_ffi_fn_method_jsruntime_execute(self.uniffiClonePointer(),
        FfiConverterString.lower(code),
        FfiConverterTypeJsConfig.lower(config),$0
    )
})
}
    

}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeJsRuntime: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = JsRuntime

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> JsRuntime {
        return JsRuntime(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: JsRuntime) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> JsRuntime {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: JsRuntime, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}




#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJsRuntime_lift(_ pointer: UnsafeMutableRawPointer) throws -> JsRuntime {
    return try FfiConverterTypeJsRuntime.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJsRuntime_lower(_ value: JsRuntime) -> UnsafeMutableRawPointer {
    return FfiConverterTypeJsRuntime.lower(value)
}




public protocol PythonRuntimeProtocol : AnyObject {
    
    func execute(code: String, config: PythonConfig) throws  -> PythonResult
    
}

open class PythonRuntime:
    PythonRuntimeProtocol {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_wasm_agent_tools_ffi_fn_clone_pythonruntime(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_wasm_agent_tools_ffi_fn_free_pythonruntime(pointer, $0) }
    }

    
public static func create()throws  -> PythonRuntime {
    return try  FfiConverterTypePythonRuntime.lift(try rustCallWithError(FfiConverterTypeWasmToolsError.lift) {
    uniffi_wasm_agent_tools_ffi_fn_constructor_pythonruntime_create($0
    )
})
}
    

    
open func execute(code: String, config: PythonConfig)throws  -> PythonResult {
    return try  FfiConverterTypePythonResult.lift(try rustCallWithError(FfiConverterTypeWasmToolsError.lift) {
    uniffi_wasm_agent_tools_ffi_fn_method_pythonruntime_execute(self.uniffiClonePointer(),
        FfiConverterString.lower(code),
        FfiConverterTypePythonConfig.lower(config),$0
    )
})
}
    

}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePythonRuntime: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = PythonRuntime

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> PythonRuntime {
        return PythonRuntime(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: PythonRuntime) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PythonRuntime {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: PythonRuntime, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}




#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePythonRuntime_lift(_ pointer: UnsafeMutableRawPointer) throws -> PythonRuntime {
    return try FfiConverterTypePythonRuntime.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePythonRuntime_lower(_ value: PythonRuntime) -> UnsafeMutableRawPointer {
    return FfiConverterTypePythonRuntime.lower(value)
}




public protocol WasmSandboxProtocol : AnyObject {
    
    func execute(wasmBytes: Data, inputJson: String, config: SandboxConfig) throws  -> SandboxResult
    
    func executeWat(watText: String, inputJson: String, config: SandboxConfig) throws  -> SandboxResult
    
}

open class WasmSandbox:
    WasmSandboxProtocol {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_wasm_agent_tools_ffi_fn_clone_wasmsandbox(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_wasm_agent_tools_ffi_fn_free_wasmsandbox(pointer, $0) }
    }

    
public static func create()throws  -> WasmSandbox {
    return try  FfiConverterTypeWasmSandbox.lift(try rustCallWithError(FfiConverterTypeWasmToolsError.lift) {
    uniffi_wasm_agent_tools_ffi_fn_constructor_wasmsandbox_create($0
    )
})
}
    

    
open func execute(wasmBytes: Data, inputJson: String, config: SandboxConfig)throws  -> SandboxResult {
    return try  FfiConverterTypeSandboxResult.lift(try rustCallWithError(FfiConverterTypeWasmToolsError.lift) {
    uniffi_wasm_agent_tools_ffi_fn_method_wasmsandbox_execute(self.uniffiClonePointer(),
        FfiConverterData.lower(wasmBytes),
        FfiConverterString.lower(inputJson),
        FfiConverterTypeSandboxConfig.lower(config),$0
    )
})
}
    
open func executeWat(watText: String, inputJson: String, config: SandboxConfig)throws  -> SandboxResult {
    return try  FfiConverterTypeSandboxResult.lift(try rustCallWithError(FfiConverterTypeWasmToolsError.lift) {
    uniffi_wasm_agent_tools_ffi_fn_method_wasmsandbox_execute_wat(self.uniffiClonePointer(),
        FfiConverterString.lower(watText),
        FfiConverterString.lower(inputJson),
        FfiConverterTypeSandboxConfig.lower(config),$0
    )
})
}
    

}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeWasmSandbox: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = WasmSandbox

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> WasmSandbox {
        return WasmSandbox(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: WasmSandbox) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> WasmSandbox {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: WasmSandbox, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}




#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWasmSandbox_lift(_ pointer: UnsafeMutableRawPointer) throws -> WasmSandbox {
    return try FfiConverterTypeWasmSandbox.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWasmSandbox_lower(_ value: WasmSandbox) -> UnsafeMutableRawPointer {
    return FfiConverterTypeWasmSandbox.lower(value)
}


public struct JsConfig {
    public var timeoutSecs: UInt64
    public var fuelLimit: UInt64
    public var maxMemoryBytes: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(timeoutSecs: UInt64, fuelLimit: UInt64, maxMemoryBytes: UInt64) {
        self.timeoutSecs = timeoutSecs
        self.fuelLimit = fuelLimit
        self.maxMemoryBytes = maxMemoryBytes
    }
}



extension JsConfig: Equatable, Hashable {
    public static func ==(lhs: JsConfig, rhs: JsConfig) -> Bool {
        if lhs.timeoutSecs != rhs.timeoutSecs {
            return false
        }
        if lhs.fuelLimit != rhs.fuelLimit {
            return false
        }
        if lhs.maxMemoryBytes != rhs.maxMemoryBytes {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(timeoutSecs)
        hasher.combine(fuelLimit)
        hasher.combine(maxMemoryBytes)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeJsConfig: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> JsConfig {
        return
            try JsConfig(
                timeoutSecs: FfiConverterUInt64.read(from: &buf), 
                fuelLimit: FfiConverterUInt64.read(from: &buf), 
                maxMemoryBytes: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: JsConfig, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.timeoutSecs, into: &buf)
        FfiConverterUInt64.write(value.fuelLimit, into: &buf)
        FfiConverterUInt64.write(value.maxMemoryBytes, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJsConfig_lift(_ buf: RustBuffer) throws -> JsConfig {
    return try FfiConverterTypeJsConfig.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJsConfig_lower(_ value: JsConfig) -> RustBuffer {
    return FfiConverterTypeJsConfig.lower(value)
}


public struct JsResult {
    public var output: String
    public var error: String?
    public var fuelConsumed: UInt64
    public var durationMs: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(output: String, error: String?, fuelConsumed: UInt64, durationMs: UInt64) {
        self.output = output
        self.error = error
        self.fuelConsumed = fuelConsumed
        self.durationMs = durationMs
    }
}



extension JsResult: Equatable, Hashable {
    public static func ==(lhs: JsResult, rhs: JsResult) -> Bool {
        if lhs.output != rhs.output {
            return false
        }
        if lhs.error != rhs.error {
            return false
        }
        if lhs.fuelConsumed != rhs.fuelConsumed {
            return false
        }
        if lhs.durationMs != rhs.durationMs {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(output)
        hasher.combine(error)
        hasher.combine(fuelConsumed)
        hasher.combine(durationMs)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeJsResult: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> JsResult {
        return
            try JsResult(
                output: FfiConverterString.read(from: &buf), 
                error: FfiConverterOptionString.read(from: &buf), 
                fuelConsumed: FfiConverterUInt64.read(from: &buf), 
                durationMs: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: JsResult, into buf: inout [UInt8]) {
        FfiConverterString.write(value.output, into: &buf)
        FfiConverterOptionString.write(value.error, into: &buf)
        FfiConverterUInt64.write(value.fuelConsumed, into: &buf)
        FfiConverterUInt64.write(value.durationMs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJsResult_lift(_ buf: RustBuffer) throws -> JsResult {
    return try FfiConverterTypeJsResult.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJsResult_lower(_ value: JsResult) -> RustBuffer {
    return FfiConverterTypeJsResult.lower(value)
}


public struct PythonConfig {
    public var timeoutSecs: UInt64
    public var fuelLimit: UInt64
    public var maxMemoryBytes: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(timeoutSecs: UInt64, fuelLimit: UInt64, maxMemoryBytes: UInt64) {
        self.timeoutSecs = timeoutSecs
        self.fuelLimit = fuelLimit
        self.maxMemoryBytes = maxMemoryBytes
    }
}



extension PythonConfig: Equatable, Hashable {
    public static func ==(lhs: PythonConfig, rhs: PythonConfig) -> Bool {
        if lhs.timeoutSecs != rhs.timeoutSecs {
            return false
        }
        if lhs.fuelLimit != rhs.fuelLimit {
            return false
        }
        if lhs.maxMemoryBytes != rhs.maxMemoryBytes {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(timeoutSecs)
        hasher.combine(fuelLimit)
        hasher.combine(maxMemoryBytes)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePythonConfig: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PythonConfig {
        return
            try PythonConfig(
                timeoutSecs: FfiConverterUInt64.read(from: &buf), 
                fuelLimit: FfiConverterUInt64.read(from: &buf), 
                maxMemoryBytes: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: PythonConfig, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.timeoutSecs, into: &buf)
        FfiConverterUInt64.write(value.fuelLimit, into: &buf)
        FfiConverterUInt64.write(value.maxMemoryBytes, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePythonConfig_lift(_ buf: RustBuffer) throws -> PythonConfig {
    return try FfiConverterTypePythonConfig.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePythonConfig_lower(_ value: PythonConfig) -> RustBuffer {
    return FfiConverterTypePythonConfig.lower(value)
}


public struct PythonResult {
    public var output: String
    public var error: String?
    public var fuelConsumed: UInt64
    public var durationMs: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(output: String, error: String?, fuelConsumed: UInt64, durationMs: UInt64) {
        self.output = output
        self.error = error
        self.fuelConsumed = fuelConsumed
        self.durationMs = durationMs
    }
}



extension PythonResult: Equatable, Hashable {
    public static func ==(lhs: PythonResult, rhs: PythonResult) -> Bool {
        if lhs.output != rhs.output {
            return false
        }
        if lhs.error != rhs.error {
            return false
        }
        if lhs.fuelConsumed != rhs.fuelConsumed {
            return false
        }
        if lhs.durationMs != rhs.durationMs {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(output)
        hasher.combine(error)
        hasher.combine(fuelConsumed)
        hasher.combine(durationMs)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePythonResult: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PythonResult {
        return
            try PythonResult(
                output: FfiConverterString.read(from: &buf), 
                error: FfiConverterOptionString.read(from: &buf), 
                fuelConsumed: FfiConverterUInt64.read(from: &buf), 
                durationMs: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: PythonResult, into buf: inout [UInt8]) {
        FfiConverterString.write(value.output, into: &buf)
        FfiConverterOptionString.write(value.error, into: &buf)
        FfiConverterUInt64.write(value.fuelConsumed, into: &buf)
        FfiConverterUInt64.write(value.durationMs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePythonResult_lift(_ buf: RustBuffer) throws -> PythonResult {
    return try FfiConverterTypePythonResult.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePythonResult_lower(_ value: PythonResult) -> RustBuffer {
    return FfiConverterTypePythonResult.lower(value)
}


public struct SandboxConfig {
    public var fuelLimit: UInt64
    public var maxMemoryBytes: UInt64
    public var timeoutSecs: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(fuelLimit: UInt64, maxMemoryBytes: UInt64, timeoutSecs: UInt64) {
        self.fuelLimit = fuelLimit
        self.maxMemoryBytes = maxMemoryBytes
        self.timeoutSecs = timeoutSecs
    }
}



extension SandboxConfig: Equatable, Hashable {
    public static func ==(lhs: SandboxConfig, rhs: SandboxConfig) -> Bool {
        if lhs.fuelLimit != rhs.fuelLimit {
            return false
        }
        if lhs.maxMemoryBytes != rhs.maxMemoryBytes {
            return false
        }
        if lhs.timeoutSecs != rhs.timeoutSecs {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(fuelLimit)
        hasher.combine(maxMemoryBytes)
        hasher.combine(timeoutSecs)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSandboxConfig: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SandboxConfig {
        return
            try SandboxConfig(
                fuelLimit: FfiConverterUInt64.read(from: &buf), 
                maxMemoryBytes: FfiConverterUInt64.read(from: &buf), 
                timeoutSecs: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: SandboxConfig, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.fuelLimit, into: &buf)
        FfiConverterUInt64.write(value.maxMemoryBytes, into: &buf)
        FfiConverterUInt64.write(value.timeoutSecs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSandboxConfig_lift(_ buf: RustBuffer) throws -> SandboxConfig {
    return try FfiConverterTypeSandboxConfig.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSandboxConfig_lower(_ value: SandboxConfig) -> RustBuffer {
    return FfiConverterTypeSandboxConfig.lower(value)
}


public struct SandboxResult {
    public var output: String
    public var fuelConsumed: UInt64
    public var durationMs: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(output: String, fuelConsumed: UInt64, durationMs: UInt64) {
        self.output = output
        self.fuelConsumed = fuelConsumed
        self.durationMs = durationMs
    }
}



extension SandboxResult: Equatable, Hashable {
    public static func ==(lhs: SandboxResult, rhs: SandboxResult) -> Bool {
        if lhs.output != rhs.output {
            return false
        }
        if lhs.fuelConsumed != rhs.fuelConsumed {
            return false
        }
        if lhs.durationMs != rhs.durationMs {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(output)
        hasher.combine(fuelConsumed)
        hasher.combine(durationMs)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSandboxResult: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SandboxResult {
        return
            try SandboxResult(
                output: FfiConverterString.read(from: &buf), 
                fuelConsumed: FfiConverterUInt64.read(from: &buf), 
                durationMs: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: SandboxResult, into buf: inout [UInt8]) {
        FfiConverterString.write(value.output, into: &buf)
        FfiConverterUInt64.write(value.fuelConsumed, into: &buf)
        FfiConverterUInt64.write(value.durationMs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSandboxResult_lift(_ buf: RustBuffer) throws -> SandboxResult {
    return try FfiConverterTypeSandboxResult.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSandboxResult_lower(_ value: SandboxResult) -> RustBuffer {
    return FfiConverterTypeSandboxResult.lower(value)
}


public enum WasmToolsError {

    
    
    case Io(msg: String
    )
    case Execution(msg: String
    )
    case IntegrityViolation(msg: String
    )
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeWasmToolsError: FfiConverterRustBuffer {
    typealias SwiftType = WasmToolsError

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> WasmToolsError {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        

        
        case 1: return .Io(
            msg: try FfiConverterString.read(from: &buf)
            )
        case 2: return .Execution(
            msg: try FfiConverterString.read(from: &buf)
            )
        case 3: return .IntegrityViolation(
            msg: try FfiConverterString.read(from: &buf)
            )

         default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: WasmToolsError, into buf: inout [UInt8]) {
        switch value {

        

        
        
        case let .Io(msg):
            writeInt(&buf, Int32(1))
            FfiConverterString.write(msg, into: &buf)
            
        
        case let .Execution(msg):
            writeInt(&buf, Int32(2))
            FfiConverterString.write(msg, into: &buf)
            
        
        case let .IntegrityViolation(msg):
            writeInt(&buf, Int32(3))
            FfiConverterString.write(msg, into: &buf)
            
        }
    }
}


extension WasmToolsError: Equatable, Hashable {}

extension WasmToolsError: Foundation.LocalizedError {
    public var errorDescription: String? {
        String(reflecting: self)
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionString: FfiConverterRustBuffer {
    typealias SwiftType = String?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterString.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterString.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

private enum InitializationResult {
    case ok
    case contractVersionMismatch
    case apiChecksumMismatch
}
// Use a global variable to perform the versioning checks. Swift ensures that
// the code inside is only computed once.
private var initializationResult: InitializationResult = {
    // Get the bindings contract version from our ComponentInterface
    let bindings_contract_version = 26
    // Get the scaffolding contract version by calling the into the dylib
    let scaffolding_contract_version = ffi_wasm_agent_tools_ffi_uniffi_contract_version()
    if bindings_contract_version != scaffolding_contract_version {
        return InitializationResult.contractVersionMismatch
    }
    if (uniffi_wasm_agent_tools_ffi_checksum_method_jsruntime_execute() != 36748) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_wasm_agent_tools_ffi_checksum_method_pythonruntime_execute() != 27925) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_wasm_agent_tools_ffi_checksum_method_wasmsandbox_execute() != 46528) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_wasm_agent_tools_ffi_checksum_method_wasmsandbox_execute_wat() != 16559) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_wasm_agent_tools_ffi_checksum_constructor_jsruntime_create() != 40926) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_wasm_agent_tools_ffi_checksum_constructor_pythonruntime_create() != 15425) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_wasm_agent_tools_ffi_checksum_constructor_wasmsandbox_create() != 53089) {
        return InitializationResult.apiChecksumMismatch
    }

    return InitializationResult.ok
}()

private func uniffiEnsureInitialized() {
    switch initializationResult {
    case .ok:
        break
    case .contractVersionMismatch:
        fatalError("UniFFI contract version mismatch: try cleaning and rebuilding your project")
    case .apiChecksumMismatch:
        fatalError("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
}

// swiftlint:enable all